import React from 'react';
import s from './Filter.module.css';

const Filter = () => {
    return(
        <wrapper rapper className= {s.filter}>
        <div>filter</div>
      </wrapper>   
    )
}

export default Filter ;